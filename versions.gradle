buildscript {
    ext {

        // Library Version
        libraryVersion = '0.0.5'

        // Kotlin
        kotlin_version = '1.3.30'

        // Gradle
        gradleBuildVersion = '3.3.2'

        // Bintray
        bintrayVersion = '1.8.4'

        // Dokka
        dokkaVersion = '0.9.18'

        // Dcendents
        dcendentsVersion = '2.1'

        // Test
        junitVersion = '4.12'
        mockitoCoreVersion = '2.24.5'
        roboelectricVersion = '4.2'

        // AndroidTest
        testExtJUnitVersion = '1.1.0'
        testCoreVersion = '1.1.0'
        testRunnerVersion = '1.1.1'
        espressoCoreVersion = '3.1.1'

        // MultiDex
        multidexVersion = '2.0.1'

        // Gson
        gsonVersion = '2.8.5'

        // AndroidX
        appCompatVersion = '1.0.2'
        recyclerViewVersion = '1.0.0'
        constraintLayoutVersion = '1.1.3'

        // Artear
        networkingVersion = '0.0.3'
        domainVersion = '0.0.5'
        coverItemVersion = '0.0.6'
        articleItemVersion = '0.0.2'
        bannerItemVersion = '0.0.5'

    }
}